<UserControl x:Class="AgyToolbox.Views.CheatSheetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="10">
        <StackPanel Margin="10">
            <TextBlock Text="Windows 高频工程快捷键与窗口调度速查表："
                       FontSize="13" Foreground="#64748B" Margin="0,0,0,16"/>

            <Border Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="8" Padding="16" Margin="0,0,0,12">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="220"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Win + V" FontSize="18" FontWeight="Bold" Foreground="#0078D4" VerticalAlignment="Center"/>
                    <StackPanel Grid.Column="1">
                        <TextBlock Text="自带全局剪贴板历史记录" FontSize="14" FontWeight="Bold" Foreground="#1E293B"/>
                        <TextBlock Text="别再装第三方剪贴板工具了！首次按下开启后，自动记录多次复制的文本和图片，支持常用文本置顶和跨设备同步。" FontSize="12" Foreground="#64748B" Margin="0,4,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>

            <Border Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="8" Padding="16" Margin="0,0,0,12">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="220"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Win + Ctrl + Shift + B" FontSize="16" FontWeight="Bold" Foreground="#DC2626" VerticalAlignment="Center"/>
                    <StackPanel Grid.Column="1">
                        <TextBlock Text="瞬间重启显卡驱动 (黑屏/掉驱动急救)" FontSize="14" FontWeight="Bold" Foreground="#1E293B"/>
                        <TextBlock Text="全屏游戏黑屏、外接显示器无信号时按一下，电脑会蜂鸣一声屏幕闪烁一次，不关机直接重载 GPU 驱动。" FontSize="12" Foreground="#64748B" Margin="0,4,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>

            <Border Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="8" Padding="16" Margin="0,0,0,12">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="220"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Win + Shift + S" FontSize="18" FontWeight="Bold" Foreground="#16A34A" VerticalAlignment="Center"/>
                    <StackPanel Grid.Column="1">
                        <TextBlock Text="全能原生截图工具" FontSize="14" FontWeight="Bold" Foreground="#1E293B"/>
                        <TextBlock Text="支持矩形选择、任意不规则形状套索、单个应用窗口、以及全屏截图，截图自动进剪贴板并支持直接涂鸦画笔标注。" FontSize="12" Foreground="#64748B" Margin="0,4,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>

            <Border Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="8" Padding="16" Margin="0,0,0,12">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="220"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Win + ." FontSize="18" FontWeight="Bold" Foreground="#8B5CF6" VerticalAlignment="Center"/>
                    <StackPanel Grid.Column="1">
                        <TextBlock Text="原生 Emoji 与特殊数学符号键盘" FontSize="14" FontWeight="Bold" Foreground="#1E293B"/>
                        <TextBlock Text="打字时随时按 Win + 句号，弹出表情包、希腊字母、数学符号、货币单位面板，无需上网搜索复制粘贴。" FontSize="12" Foreground="#64748B" Margin="0,4,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>

            <Border Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="8" Padding="16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="220"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Ctrl + Shift + Esc" FontSize="18" FontWeight="Bold" Foreground="#F59E0B" VerticalAlignment="Center"/>
                    <StackPanel Grid.Column="1">
                        <TextBlock Text="一步秒开任务管理器" FontSize="14" FontWeight="Bold" Foreground="#1E293B"/>
                        <TextBlock Text="不要再按 Ctrl + Alt + Del 慢慢选任务管理器了，单手按下秒开，卡顿假死排查快人一步。" FontSize="12" Foreground="#64748B" Margin="0,4,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
