<UserControl x:Class="AgyToolbox.Views.PortableToolboxView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:views="clr-namespace:AgyToolbox.Views"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1000">

    <UserControl.Resources>
        <Style x:Key="SubTabItemStyle" TargetType="TabItem">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="14,8"/>
            <Setter Property="Margin" Value="0,0,4,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Foreground" Value="#64748B"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="SubTabBorder" Background="#F1F5F9" CornerRadius="6,6,0,0" Padding="{TemplateBinding Padding}" Margin="{TemplateBinding Margin}">
                            <ContentPresenter x:Name="SubContentSite" VerticalAlignment="Center" HorizontalAlignment="Center" ContentSource="Header"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="SubTabBorder" Property="Background" Value="White"/>
                                <Setter Property="Foreground" Value="#0284C7"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="SubTabBorder" Property="Background" Value="#E2E8F0"/>
                            </Trigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsSelected" Value="True"/>
                                    <Condition Property="IsMouseOver" Value="True"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="SubTabBorder" Property="Background" Value="White"/>
                            </MultiTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Background="#F8FAFC">
        <TabControl Margin="8" Background="White" BorderBrush="#E2E8F0" BorderThickness="1"
                    ItemContainerStyle="{StaticResource SubTabItemStyle}">

            <!-- 1. 局域网快传 -->
            <TabItem Header="📡 局域网跨设备快传 (WebDrop)">
                <views:WebDropView />
            </TabItem>

            <!-- 2. 大文件极速猎手 -->
            <TabItem Header="🔍 大文件极速猎手 (DiskHunter)">
                <views:DiskHunterView />
            </TabItem>

            <!-- 3. 硬件与网卡透视 -->
            <TabItem Header="📊 硬件与网卡透视 (SysInfo)">
                <views:SysInfoView />
            </TabItem>

            <!-- 4. 开发者离线军刀 -->
            <TabItem Header="🧰 开发者离线军刀 (DevTools)">
                <views:DevToolsView />
            </TabItem>

            <!-- 5. DNS 测速与镜像源 -->
            <TabItem Header="🌐 DNS 测速与国内镜像 (NetMirrors)">
                <views:NetMirrorsView />
            </TabItem>

        </TabControl>
    </Grid>
</UserControl>
