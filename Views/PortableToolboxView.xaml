<UserControl x:Class="AgyToolbox.Views.PortableToolboxView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:views="clr-namespace:AgyToolbox.Views"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1000">

    <Grid Background="{DynamicResource BrushWindowBg}">
        <TabControl Margin="8" Background="{DynamicResource BrushCardBg}" BorderBrush="{DynamicResource BrushCardBorder}" BorderThickness="1"
                    ItemContainerStyle="{DynamicResource SubTabItemStyle}">

            <!-- 1. 局域网快传 -->
            <TabItem Header="📡 局域网跨设备快传 (WebDrop)">
                <views:WebDropView />
            </TabItem>

            <!-- 2. 大文件极速猎手 -->
            <TabItem Header="🔍 大文件极速猎手 (DiskHunter)">
                <views:DiskHunterView />
            </TabItem>

            <!-- 3. 硬件与网卡透视 -->
            <TabItem Header="📊 硬件与网卡透视 (SysInfo)">
                <views:SysInfoView />
            </TabItem>

            <!-- 4. 开发者离线军刀 -->
            <TabItem Header="🧰 开发者离线军刀 (DevTools)">
                <views:DevToolsView />
            </TabItem>

            <!-- 5. DNS 测速与镜像源 -->
            <TabItem Header="🌐 DNS 测速与国内镜像 (NetMirrors)">
                <views:NetMirrorsView />
            </TabItem>

        </TabControl>
    </Grid>
</UserControl>
